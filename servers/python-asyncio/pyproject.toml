[project]
name = "python-asyncio-http-server"
version = "0.1.0"
description = "Dependency-free Python asyncio HTTP/1.1 benchmark server"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "Alexander Zelinsky" }]
dependencies = []

[tool.ruff]
target-version = "py312"
line-length = 100
